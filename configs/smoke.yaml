case_name: smoke
data:
  mode: synthetic
  output_dir: outputs/smoke
  n_cells: 24
  n_genes: 48
  n_structures: 3
  image_size: 32
  region_id_key: contour_id
  min_cells_per_region: 1
  seed: 13
model:
  d_model: 32
  n_heads: 4
  n_layers: 1
  max_genes: 32
  n_expression_bins: 16
  image_size: 32
  patch_scales: [1, 2]
  max_cells_per_region: 16
  use_cell_context: true
  dropout: 0.1
training:
  batch_size: 6
  learning_rate: 0.001
  lr_schedule: cosine
  warmup_steps: 1
  max_steps: 2
  save_every_n_steps: 1
  mask_probability: 0.2
  neighborhood_k: 4
  output_dir: outputs/smoke/train
  device: cpu
  num_workers: 0
